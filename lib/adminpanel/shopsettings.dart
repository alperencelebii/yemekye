import 'package:flutter/material.dart';

class ShopSettings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Mağaza Ayarları")),
      body: Center(
        child: Text("Mağaza ayarları burada düzenlenecek."),
      ),
    );
  }
}
